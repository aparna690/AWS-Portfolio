The following screenshots document the complete deployment process of the custom web template on an EC2 instance created in Amazon Web Services. They provide visual confirmation of server configuration, secure file transfer, web server setup, and the final website output.

The images include the running Amazon EC2 instance, security group settings for SSH and HTTP access, connection through WinSCP, Apache server configuration on Amazon Linux, and the successfully hosted web template displayed in a browser.

These screenshots serve as evidence of practical cloud deployment and system configuration.
